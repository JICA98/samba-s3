# RPCSX engine Android integration edits live in the samba-s3-core submodule
# on branch samba-android, including 2b074811c (engine worktree) and
# 829cd6582 (out-of-tree samba-build-id CMake). Later samba-android commits
# supersede these pins.
#
# This parent patch is intentionally empty of git-diff hunks so build_rpcsx.sh
# skips apply. Do not regenerate a full diff of already-committed engine code.
# New engine edits belong in the submodule; keep this file comment-only unless
# a real git diff is required for an uncommitted engine change.
