diff --git a/android/src/rpcsx-android.cpp b/android/src/rpcsx-android.cpp
index 8308a4798..4b92af31a 100644
--- a/android/src/rpcsx-android.cpp
+++ b/android/src/rpcsx-android.cpp
@@ -1648,7 +1648,8 @@ extern void ppu_precompile(std::vector<std::string> &dir_queue,
                            std::vector<ppu_module<lv2_obj> *> *loaded_prx);
 extern bool ppu_initialize(const ppu_module<lv2_obj> &, bool check_only = false,
                            u64 file_size = 0);
-extern void ppu_finalize(const ppu_module<lv2_obj> &);
+extern void ppu_finalize(const ppu_module<lv2_obj> &,
+                         bool force_mem_release = false);
 extern bool ppu_load_rel_exec(const ppu_rel_object &);
 
 class CompilationQueue {
@@ -1765,8 +1766,15 @@ private:
     // captures this context when its thread starts; setting it later around
     // ppu_precompile can misclassify install PPU work as runtime work and
     // create the second (2000) notification.
+    // Gate C: Demon's Souls / large titles OOM around ~99 LLVM objs even with
+    // 1 worker. Bound each ppu_initialize to 32 new objs, finalize+retry until
+    // cache-complete. Kotlin still sees one INSTALL job / FGS 3000.
+    // A1/Gate-C evidence: even llvm=1 OOMs near ~99 objs; 32-obj steps still
+    // SIGABRT'd once on device — keep steps smaller so finalize can reclaim.
+    constexpr u32 kInstallStepMaxNewModules = 16;
     SystemProgressContext installProgressContext{
-        CompileOrigin::INSTALL, allocate_compile_job_id(), pendingInstallTitleId};
+        CompileOrigin::INSTALL, allocate_compile_job_id(), pendingInstallTitleId,
+        kInstallStepMaxNewModules};
     set_system_progress_context(installProgressContext);
     struct InstallProgressContextGuard {
       ~InstallProgressContextGuard() { clear_system_progress_context(); }
@@ -1838,8 +1846,22 @@ private:
                         _main.applied_patches, std::vector<u32>{})) {
         Emu.ConfigurePPUCache();
         Emu.SetTestMode();
-        rpcsx_android.error("Going to precompile main PPU module");
-        ppu_initialize(_main);
+        rpcsx_android.error("Going to precompile main PPU module (multistep)");
+        // Cache-driven steps: each call compiles ≤install_step_max_new new objs,
+        // persists them, then ppu_finalize releases JIT module state before the
+        // next step. Never marks COMPLETED until no more_work remains.
+        for (u32 step = 0;; ++step) {
+          set_ppu_install_step_more_work(false);
+          ppu_initialize(_main);
+          const bool more = get_ppu_install_step_more_work();
+          ppu_finalize(_main, true);
+          rpcsx_android.notice(
+              "S3PPU install_step=%u more_work=%d title=%s", step,
+              more ? 1 : 0, pendingInstallTitleId);
+          if (!more) {
+            break;
+          }
+        }
         mod_list.emplace_back(&_main);
       }
     }
diff --git a/rpcs3/Emu/Cell/PPUThread.cpp b/rpcs3/Emu/Cell/PPUThread.cpp
index d2768d8b0..89ec8a0f9 100644
--- a/rpcs3/Emu/Cell/PPUThread.cpp
+++ b/rpcs3/Emu/Cell/PPUThread.cpp
@@ -16,6 +16,7 @@
 #include "Emu/VFS.h"
 #include "Emu/system_progress.hpp"
 #include "Emu/system_utils.hpp"
+#include "Emu/compile_progress.hpp"
 #include "Emu/System.h"
 #include "PPUThread.h"
 #include "PPUInterpreter.h"
@@ -132,7 +133,19 @@ namespace
 				? std::max<u32>(2, std::min<u32>(6, hardware_threads - 2))
 				: hardware_threads;
 		const u32 policy_limit = policy == ppu_compile_policy::foreground ? foreground_candidate : max_threads;
-		return std::min<u32>(::narrow<u32>(workload_size), std::min(max_threads, policy_limit));
+		u32 effective = std::min<u32>(::narrow<u32>(workload_size), std::min(max_threads, policy_limit));
+
+#if defined(__ANDROID__)
+		// INSTALL-origin only: never raise a user-requested lower limit; cap concurrent
+		// aux JIT arenas (3×256MiB each) that cause Scudo map / bad_alloc on large titles.
+		// Runtime / prelaunch keep the user's Max LLVM Compile Threads unchanged.
+		if (get_system_progress_context().origin == CompileOrigin::INSTALL)
+		{
+			constexpr u32 kAndroidInstallSafeCap = 2;
+			effective = std::min(effective, kAndroidInstallSafeCap);
+		}
+#endif
+		return effective;
 	}
 
 	static jit_codegen_level ppu_jit_codegen_level()
@@ -4841,7 +4854,17 @@ extern void ppu_precompile(std::vector<std::string>& dir_queue, std::vector<ppu_
 					{
 						obj.clear(), src.close(); // Clear decrypted file and elf object memory
 						file_allocation = {};     // release used file memory
-						ppu_initialize(*prx, false, file_size, memory_limit);
+						for (;;)
+						{
+							set_ppu_install_step_more_work(false);
+							ppu_initialize(*prx, false, file_size, memory_limit);
+							const bool more = get_ppu_install_step_more_work();
+							if (!more)
+							{
+								break;
+							}
+							ppu_finalize(*prx, true);
+						}
 						ppu_finalize(*prx, true);
 						continue;
 					}
@@ -4887,7 +4910,17 @@ extern void ppu_precompile(std::vector<std::string>& dir_queue, std::vector<ppu_
 							break;
 						}
 
-						ppu_initialize(*ovlm, false, file_size, memory_limit);
+						for (;;)
+						{
+							set_ppu_install_step_more_work(false);
+							ppu_initialize(*ovlm, false, file_size, memory_limit);
+							const bool more = get_ppu_install_step_more_work();
+							if (!more)
+							{
+								break;
+							}
+							ppu_finalize(*ovlm, true);
+						}
 						ppu_finalize(*ovlm, true);
 						break;
 					}
@@ -5021,7 +5054,17 @@ extern void ppu_precompile(std::vector<std::string>& dir_queue, std::vector<ppu_
 
 						_main.name = ' '; // Make ppu_finalize work
 						Emu.ConfigurePPUCache();
-						ppu_initialize(_main, false, file_size, memory_limit);
+						for (;;)
+						{
+							set_ppu_install_step_more_work(false);
+							ppu_initialize(_main, false, file_size, memory_limit);
+							const bool more = get_ppu_install_step_more_work();
+							if (!more)
+							{
+								break;
+							}
+							ppu_finalize(_main, true);
+						}
 						spu_cache::initialize(false);
 						ppu_finalize(_main, true);
 						_main = {};
@@ -5336,6 +5379,14 @@ bool ppu_initialize(const ppu_module<lv2_obj>& info, bool check_only, u64 file_s
 
 	bool has_mfvscr = false;
 
+#if defined(__ANDROID__)
+	// INSTALL Gate C progress: full module count (cached + uncached) so UI shows
+	// real "module X of TOTAL", not the per-step batch size.
+	u32 install_modules_total = 0;
+	u32 install_uncached_remaining = 0;
+	u32 install_cached_count = 0;
+#endif
+
 	const bool is_being_used_in_emulation = vm::base(info.segs[0].addr) == info.segs[0].ptr;
 	const ppu_compile_policy compile_policy = is_being_used_in_emulation
 		? ppu_compile_policy::background
@@ -5895,6 +5946,16 @@ bool ppu_initialize(const ppu_module<lv2_obj>& info, bool check_only, u64 file_s
 
 		module_counter++;
 
+#if defined(__ANDROID__)
+		{
+			const auto ctx = get_system_progress_context();
+			if (ctx.origin == CompileOrigin::INSTALL && ctx.install_step_max_new > 0)
+			{
+				install_modules_total++;
+			}
+		}
+#endif
+
 		if (!check_only)
 		{
 			link_workload.emplace_back(obj_name, false);
@@ -5903,6 +5964,15 @@ bool ppu_initialize(const ppu_module<lv2_obj>& info, bool check_only, u64 file_s
 		// Check object file
 		if (jit_compiler::check(cache_path + obj_name))
 		{
+#if defined(__ANDROID__)
+			{
+				const auto ctx = get_system_progress_context();
+				if (ctx.origin == CompileOrigin::INSTALL && ctx.install_step_max_new > 0)
+				{
+					install_cached_count++;
+				}
+			}
+#endif
 			if (!is_being_used_in_emulation && !check_only)
 			{
 				ppu_log.success("LLVM: Module exists: %s", obj_name);
@@ -5917,6 +5987,24 @@ bool ppu_initialize(const ppu_module<lv2_obj>& info, bool check_only, u64 file_s
 			return true;
 		}
 
+#if defined(__ANDROID__)
+		// INSTALL Gate C: count every uncached module for real progress totals, but
+		// only enqueue up to install_step_max_new into this step's workload.
+		{
+			const auto ctx = get_system_progress_context();
+			if (ctx.origin == CompileOrigin::INSTALL && ctx.install_step_max_new > 0)
+			{
+				install_uncached_remaining++;
+				if (workload.size() >= ctx.install_step_max_new)
+				{
+					set_ppu_install_step_more_work(true);
+					link_workload.pop_back();
+					continue; // keep counting remaining uncached for overall total
+				}
+			}
+		}
+#endif
+
 		// Remember, used in ppu_initialize(void)
 		compiled_new = true;
 
@@ -5945,7 +6033,41 @@ bool ppu_initialize(const ppu_module<lv2_obj>& info, bool check_only, u64 file_s
 		atomic_t<u64> work_done = 0;
 
 		// Update progress dialog
+#if defined(__ANDROID__)
+		{
+			const auto ctx = get_system_progress_context();
+			if (ctx.origin == CompileOrigin::INSTALL && ctx.install_step_max_new > 0)
+			{
+				// Full title total (cached + uncached). Credit cache hits once so the
+				// bar reflects real progress, not the 32-module step window.
+				const u32 overall = install_modules_total
+					? install_modules_total
+					: (+g_progr_pdone + install_uncached_remaining);
+				const u32 cur_total = +g_progr_ptotal;
+				if (overall > cur_total)
+				{
+					g_progr_ptotal += (overall - cur_total);
+				}
+				// On the first step of a session, advance pdone for already-cached objs.
+				if (+g_progr_pdone == 0 && install_cached_count > 0)
+				{
+					g_progr_pdone += install_cached_count;
+				}
+				ppu_log.notice(
+					"S3PPU install_progress pdone=%u ptotal=%u step_workload=%zu "
+					"modules_total=%u cached=%u uncached_remaining=%u more=%d",
+					+g_progr_pdone, +g_progr_ptotal, workload.size(),
+					install_modules_total, install_cached_count,
+					install_uncached_remaining, get_ppu_install_step_more_work() ? 1 : 0);
+			}
+			else
+			{
+				g_progr_ptotal += ::size32(workload);
+			}
+		}
+#else
 		g_progr_ptotal += ::size32(workload);
+#endif
 
 		*progress_dialog = get_localized_string(localized_string_id::PROGRESS_DIALOG_COMPILING_PPU_MODULES);
 
diff --git a/rpcs3/Emu/compile_progress.cpp b/rpcs3/Emu/compile_progress.cpp
index dc7a56339..416d3e4ec 100644
--- a/rpcs3/Emu/compile_progress.cpp
+++ b/rpcs3/Emu/compile_progress.cpp
@@ -50,6 +50,17 @@ u64 allocate_compile_job_id()
 static std::mutex g_ctx_mutex;
 static SystemProgressContext g_current_ctx{CompileOrigin::RUNTIME, 0, ""};
 static bool g_has_ctx = false;
+static std::atomic<bool> g_ppu_install_step_more_work{false};
+
+void set_ppu_install_step_more_work(bool more)
+{
+    g_ppu_install_step_more_work.store(more, std::memory_order_release);
+}
+
+bool get_ppu_install_step_more_work()
+{
+    return g_ppu_install_step_more_work.load(std::memory_order_acquire);
+}
 
 void set_system_progress_context(const SystemProgressContext& ctx)
 {
@@ -76,6 +87,7 @@ void clear_system_progress_context()
     std::lock_guard<std::mutex> lock(g_ctx_mutex);
     g_current_ctx = SystemProgressContext{CompileOrigin::RUNTIME, 0, ""};
     g_has_ctx = false;
+    g_ppu_install_step_more_work.store(false, std::memory_order_release);
 }
 
 // --- Snapshot builder ---
diff --git a/rpcs3/Emu/compile_progress.hpp b/rpcs3/Emu/compile_progress.hpp
index 9210b5d10..7b4e6a9e7 100644
--- a/rpcs3/Emu/compile_progress.hpp
+++ b/rpcs3/Emu/compile_progress.hpp
@@ -66,12 +66,18 @@ struct SystemProgressContext
     CompileOrigin origin = CompileOrigin::RUNTIME;
     u64 jobId = 0;
     std::string titleId; // associated title for install jobs; empty => Emu.GetTitleID() fallback
+    /** INSTALL only: max newly-compiled LLVM objs per ppu_initialize call (0 = unlimited). */
+    u32 install_step_max_new = 0;
 };
 
 void set_system_progress_context(const SystemProgressContext& ctx);
 SystemProgressContext get_system_progress_context();
 void clear_system_progress_context();
 
+/** Set when an INSTALL-bounded ppu_initialize left uncached modules for a later step. */
+void set_ppu_install_step_more_work(bool more);
+bool get_ppu_install_step_more_work();
+
 // Helper that computes PpuProgressSnapshot exactly once, preserving time_left_queue
 // smoothing state. Both the RSX UI and the Android bridge consume the same snapshot.
 // Must be called before the show_overlay_message early-continue so the Android path
