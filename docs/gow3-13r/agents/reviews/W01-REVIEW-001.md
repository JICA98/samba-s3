# W01-REVIEW-001 — independent host evaluator review

**Decision: REJECT** the exact W01-BUILD-001 method for integration or use as a real-60 scoring gate. This is a method-only verdict. The current pinned acceptance contract still has null game revision and executable hash, so its actual CLI cannot produce a frame-gate PASS today; the failures below are latent false-PASS paths exposed by a clearly labeled in-memory simulation of those two fields being resolved. No APK/core build, device capture, GOW3 FPS result, save/load test, or campaign success is claimed or approved.

| Reviewed artifact | SHA-256 |
|---|---|
| W01-REVIEW-001 task / canonical reviewer prompt | c9d1c7229b9f89ec8dff8a70ada84bd7b615be55b058b042056f109040117a09 / 145a16d95f268704795e9162a4a0d264b2a236b9d2f161249b779bba559d8d48 |
| Frozen acceptance / candidate baseline / source manifest | 7a58413ce32c82557d12272b7e4bd5d4df686667d1df08856042e0217e91c5b2 / 4f5eed78ba7c80c09b8243603966926c5a5a4004fd3578765fa3d3d1ecf32a59 / f34eef4eef0e1926012bfd7be922d8d10bf9a9630a841b392a7efa51eba62145 |
| W01-BUILD-001 source.patch / result.json | 918f8d63bbd3a0b7ef398de34045c458bd0708faaf7ea590510f2bad68633438 / 90e847f4a9bec369d56e5a952897eae120952aafa6a385191b2ed8cd96c6ec0b |
| W01-BUILD-001 SHA256SUMS / command log / producer gap | 0a499bf13c2ef255fb73e6c625d2c9ba1bb40d615e93719217b0b043ba3b6d5f / 0a5509d482692cb951f3d6a45d7b3008ef3165811f7943758392b18f90cd97d6 / 7bd8a42202b79900ac142936211bd2a1a8960b1e69e166756863eb0051e4f0a0 |
| Reviewer-owned adversarial probe / exact output | e3123b1506286fd7963d799ea621073cebb42c4c68cfb3c39ca3ee4eae388f6d / 2e1d4c48ab8f760834ce1c208d22b1662b614ff01d54e5925f87b2855b8f7c79 |

The isolated worktree is at base 8c95cb5b27a54d72328b993cd27fd8d5947c794d. Its complete scripts/perf binary diff exactly matches source.patch SHA-256 918f8d63…; I inspected the implementation and all focused tests. Re-running the 12 tests returned exit 0. The no-argument summarizer returned JSON INCONCLUSIVE with exit 2 and no fabricated demo numbers. Distinct guest/render/present IDs, duplicate/stale timestamps, surface/session changes, explicit synthetic markers, interval arithmetic, and overflow bounds have useful checks. For a *complete single scored segment*, the slowest-ceil(1%) calculation and nearest-rank p99 computation follow the registered formulas. Those positives do not cure the following false-PASS paths.

1. **Invented source can become PASS after identity resolution.** evaluate_run.py:95-130, 273-297 and 416-423 validate supplied strings/flags and reject only a short blacklist of synthetic markers; an absent data_origin is accepted. The public function also trusts a caller-supplied expected contract hash rather than binding the object itself. A completely invented 180-second, 10,800-frame record with syntactically valid hashes and monotonically increasing self-reported IDs returned PASS in the reviewer probe. The CLI does hash the current acceptance bytes (lines 442-453), and the unresolved fields correctly make that exact contract INCONCLUSIVE. However neither the method nor the absent native producer proves that these IDs represent newly produced useful guest frames, completed renders and successful presents. The producer-gap note correctly discloses the missing native stream; a positive source/provenance gate is required before the method can ever return a device frame PASS.

2. **Contract identity mismatch is accepted.** evaluate_run.py:109-130 checks only that contract revision/executable are non-null and that the run has a nonempty revision and a hex-shaped executable hash. It compares title ID but does not compare run revision or executable hash with the contract. With simulated contract revision verified-revision and executable hash b…b, a run claiming wrong-revision and c…c still returned PASS. Actual installed APK/core/driver/patch/settings hashes are likewise only shape-checked, not corroborated against trusted capture metadata.

3. **A hidden full-session stall can be omitted.** evaluate_run.py:188-210 does not require phase segments to be ordered, nonoverlapping or exhaustive over the run; lines 300-307 reset interval validation at every segment boundary; lines 321-355 score only segment-internal intervals and durations. Two 90-second scored gameplay segments separated by an uncovered 30-second hole returned PASS at 60.0 FPS, with no recorded long interval. Guest time progressed for only about 180 seconds in a 210-second run, yet the claimed guest_progress_ratio=1.0 was accepted because lines 181-186 range-check the self-report rather than deriving verified progress from guest and monotonic time. Predeclared loading/video exclusions and a complete unfiltered timeline are not enforced; slow gameplay can be silently excluded.

4. **The rolling-window gate can be skipped.** evaluate_run.py:356-363 evaluates windows only within segments at least five seconds long; lines 403-404 permit rolling_min_fps=null. A 180-second scored repeat split into 180 one-second scored gameplay segments returned PASS with no rolling window evaluated. The current frame-ending window count also subtracts one from the frames present (line 362), so boundary arithmetic needs an explicit specification and test even after the missing-window loophole is closed.

5. **Evidence manifest is internally inconsistent.** sha256sum -c W01-BUILD-001/SHA256SUMS exited 1. The manifest lists worktree/scripts/perf/summarize-run.py as 26a0f8abf4a77564b0d4d8c8b1a6f198e2b8615d31da2e5f9729591bc9bbf606; the actual file is 26a0f8abf4a77564b0b4d8c8b1a6f198e2b8615d31da2e5f9729591bc9bbf606. Every other listed entry passed, and the exact source.patch still matches the worktree diff. This appears to be a one-character manifest error, but it must be corrected and rehashed, not waived.

Required correction before another method review: bind accepted title revision/executable and all scoring identities to independently captured provenance; make absent/untrusted source metadata INCONCLUSIVE, with a verified native guest/render/present producer and immutable export completeness before any frame PASS. Validate exhaustive predeclared phase topology and raw-record coverage across the entire session, count or explicitly classify boundary intervals, derive guest speed from verified timing, and forbid PASS when any required five-second gameplay window is unscored or unevaluated. Add adversarial tests for the four probes above plus unscored gameplay, re-polled/dropped-tail records, counter overflow and invalid energy sensors; repair SHA256SUMS. Power samples are currently self-declared and merely labeled AVAILABLE_NOT_YET_INTEGRATED (evaluate_run.py:59-92); no calibrated energy integration or efficiency result exists.

Separate verdicts: **method REJECT; correctness INCONCLUSIVE; performance INCONCLUSIVE; save/load UNTESTED; efficiency UNVERIFIED; campaign INCOMPLETE.** The original whole-app and dirty-core diff hashes remain 0d92e24372b090fa9c0ace8b5b7811ff33f669969ee6424df8cbbfa25f97f14c and ff3ecb0cc8d050a6935f6b499d72ff742d64b084affe5f4a677dce478f26ddda. This review made no implementation, network, build or device change.
