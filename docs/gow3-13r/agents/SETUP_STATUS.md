# Agent setup status

The actual runner is Codex CLI 0.156.1. Child creation supports an explicit model and reasoning-effort override when a bounded fork is used. Trusted read-only verification joins `thread_spawn_edges` to `threads` in the local Codex state database; prompt text and this registry are not used as model proof. By explicit user direction, the foreground parent coordinating thread now owns orchestration. Its verified binding is `openai/gpt-6-sol/xhigh`, a documented deviation from the requested Sol Max that cannot be changed mid-session. The former child orchestrator and independent reviewer are distinct `openai/gpt-6-sol/max` threads; the former child has completed and no longer dispatches. Completed discovery agents and the existing build/tester workers are verified `openai/gpt-6-luna/high`. Full runtime handles remain in private runner state.

The reviewer completed a scoped W00 review with `NEEDS_EVIDENCE`: source baseline build is blocked by the generated build-ID patch guard; no recoverable device backup, installed/loaded artifact identity or scored frame-source evidence exists. A second review of the exact acceptance hash returned `PASS_FOR_PRE_REGISTRATION` for criteria fidelity, while overall readiness remains `NEEDS_EVIDENCE`. Neither verdict approves the old APK or a campaign result. See `reviews/W00-REVIEW-002.md`.

Only the orchestrator writes global status, registry, acceptance, task queue and decisions. Planned roles are `DEFINED` until an actual child thread with its exact role prompt and trusted binding is recorded. The completed provenance probe does not count as a build/metrics implementation worker. The completed metric source audit is a partial performance-analyst discovery result, not a measured bottleneck map. A distinct build/metrics worker has now been spawned for W00-BUILD-001 and its `openai/gpt-6-luna/high` binding verified through trusted state.

The build/metrics worker completed W00-BUILD-001 in a private isolated worktree; independent Sol Max review rejected it, and it produced no APK or device result. The original submodule registration was restored with app/core source hashes unchanged. The same verified Luna High worker is correcting the guard and portable source bundle in W00-BUILD-002, still host-only. The verified Luna High tester completed W00-TESTER-PREP-001 host-only, then acquired an OS-held physical-phone/shared-ADB lock for W00-TESTER-001. It is performing read-only identity and backup gates; no launch or completed backup has yet been claimed. No collector session exists.

The immutable W00-BUILD-001 task packet records `READY_NOT_DISPATCHED` as its creation snapshot. Later `DISPATCHED` and worker `RUNNING` events are recorded in the append-only task index and registry; the packet itself was not rewritten after dispatch. Future packets should use a lifecycle-free input contract to avoid this ambiguity.

Explicit orchestrator, reviewer and active build-worker handshakes are persisted in `handshakes.json`. The completed analyst supplied a retrospective acknowledgment, but it remains partial because no formal pre-dispatch packet/output directory was supplied; the provenance probe remains auxiliary. Parent-reported device preflight observations have a private preliminary record, but no raw command archive; the tester must recapture them under lease.
