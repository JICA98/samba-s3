# Campaign decisions

| ID | Decision | Evidence and reason |
|---|---|---|
| D001 | Scope device work to the verified OnePlus 13R and `BCUS98111`. | This campaign's target supersedes the root `AGENTS.md` instruction to update both phones after a release build. The Poco is outside this run. |
| D002 | Preserve the existing dirty app and native core; create a reproducible source identity before a source build. | Two tracked app source files and 40 tracked core files differ from their pinned commits. The core object is present locally. |
| D003 | Classify the preexisting release APK as an unattributed artifact and keep the accepted candidate null. | Its SHA-256 is known, but dirty-source build provenance, installed identity and loaded core have not been verified. |
| D004 | Require guest/render/present frame correlation and a validated raw-frame evaluator before a scored FPS result. | The present/surface callback counter and 250–1000 ms snapshots do not prove unique useful game frames; the current summarizer contains demo values. |
| D005 | Assign the patch-guard repair to the Luna build/metrics lane, with independent Sol review before source-built baseline use. | The full patch reverse check fails only on the generated build-ID file; build script exits before stamping/compiling. |
| D006 | Issue one exclusive physical-device and shared-ADB-server lease to the Luna tester after readiness; store serial and active lease privately. | JSON status alone cannot serialize concurrent device commands; collector remains read-only on a declared schedule. |
| D007 | Pre-register the WORKER.md numerical and correctness gates in `acceptance.json` before any scored experiment. | Changes to thresholds cannot retroactively promote failure. |
| D008 | Record independent acceptance fidelity `PASS_FOR_PRE_REGISTRATION` at SHA-256 `7a58413ce32c82557d12272b7e4bd5d4df686667d1df08856042e0217e91c5b2`; leave W00 readiness `NEEDS_EVIDENCE`. | Separate Sol Max reviewer W00-REVIEW-002 verified criteria fidelity only; no candidate, artifact, device or performance approval. |
| D009 | Foreground `/root` owns orchestration despite its verified GPT-6 Sol xhigh effort; the former Sol Max orchestration child is advisory/completed. | Explicit user direction supersedes the pack's requested foreground routing; this is documented as a model-effort deviation, not silently represented as Max. |
| D010 | Reject W00-BUILD-001 for candidate promotion and reuse the same Luna worker for W00-BUILD-002. | Independent Sol Max review found false-success and mutation guard paths, generated-only patch acceptance, a nonportable core base and metadata mismatch. Original Git submodule registration was restored without source changes. |
| D011 | Allow the leased Luna tester to perform backup-first read-only inventory and, only if all gates pass, one observational GOW3 launch and canonical logs. | User explicitly requested launch/logs. The device-test skill limits launch/input/evidence path; W00-TESTER-001 retains data, thermal, identity and exclusive-lock stop gates. |
| D012 | Keep the installed APK as an untouched separate observational baseline; do not install the different host APK. | The leased read-only pull hashes to `c4bf9c1f…`, unlike the host release `3bafe890…`; a historical on-phone GOW3 session ended in a no-new-frame timeout, which is context rather than a fresh run result. |
| D013 | Permit one bounded observational launch of the current installation after accessible-data backup verification, with the app-private driver/preferences rollback gap explicitly retained. | After disclosure that startup may change inaccessible private driver state, the user again requested device testing and reported ISO/PPU readiness. This directs a narrow test, not installation, driver/settings changes, save/load stress or a claim of full rollback. The tester must reacquire the OS lock, verify identity/thermals, launch only through the canonical script, and collect fresh logs once. |
| D014 | Accept W00-BUILD-002 only as host-build input readiness, not as a candidate artifact or performance result. | Independent Sol Max W00-REVIEW-003 approved exact guard/source-bundle hashes at review SHA-256 `e829900ea94505fa9554f16f5f8b32ef79bd844210f84dffe6a7e93e27380af9`; missing nested build dependencies and actual compile remain future gates. |
| D015 | Invalidate the prelaunch tester run after an external ADB actor bypassed the phone/server lock; do not cancel that other healthy agent or pretend a quiet moment proves exclusivity. | Host process observation saw Back input/screenshot/logcat from a live `scrapstorm` `agy` session on the same USB serial. Tester issued no launch, stopped device commands and released the lease at 13:07:58 UTC; a fresh coordinated window is required. |
| D016 | Treat the user's new report that GOW3 is playing as a live-session observation, not a scored measurement; do not relaunch or send input. | The separate Scrapstorm process remains alive. Ask for coordinated phone access before one passive capture; read-only evidence must distinguish this user-run session from the invalidated tester run. |
| D017 | Reuse the existing Luna High build/metrics worker for a host-only strict evaluator task, W01-BUILD-001. | W00 source audit identified demo fallback and presentation-only counters; packet SHA-256 `3757c9dbb10bb31d2eb7f3167e1d3aec630b1875b1b3453f40a9a1320d2bcecf`, no device/network/build scope. |
| D018 | Start a distinct Luna High log collector on host-only capture preparation, without access to the phone yet. | Trusted runtime confirms the binding; packet SHA-256 `fc67869d9bdef8224b65699b26296014ec3f400a8b0f333818146cb1213213b5`. This keeps evidence planning separate from the tester and does not duplicate any healthy agent. |
| D019 | Keep the current GOW3 session intact and defer all phone reads until the external ADB actor is coordinated away; take the full canonical diagnostic bundle once after a natural safe pause. | The log collector's host-only audit found that even a light PID/logcat read cannot establish a clean session boundary while the outside actor may still send input. `get-samba-logs.sh` includes heavier `top`/`dumpsys` calls unsuitable for a scored timing window. |
| D020 | Dispatch the existing Luna High tester for W00-TESTER-002 passive capture after the user's explicit coordination, while treating the outside parent process as live until observed terminal. | The user answered that the other agent is stopped and asked for testing/logs. Host process check still shows its idle parent but no ADB child. Fresh lock and current identity/thermal revalidation are required; any renewed outside ADB action invalidates the run. No relaunch/input/install/save-load is authorized in this task. |
| D021 | Reject W01-BUILD-001 for scoring integration and return the method to the Luna worker for correction. | Independent Sol Max W01-REVIEW-001 found four latent false-PASS paths (invented source, mismatched game identity, omitted timeline gap, skipped rolling window) and an invalid checksum-manifest entry. Twelve focused tests passing do not override these findings. |
| D022 | Honor the user's new request for scripted game control in a separate bounded tester task, not by silently expanding the read-only W00-TESTER-002 packet. | The current tester lease is gathering diagnostic identity/log evidence. `scripts/debug-pad.sh` is the canonical controller; a launch, if needed, must use `scripts/debug-launch-game.sh` after a fresh current-state and thermal gate. No save/load/settings/install stress is implied. |
| D023 | Preserve one canonical log bundle for the user-run GOW3 failure before any relaunch/controller retry. | W00-TESTER-002 found the latest GOW3 manifest terminal `FAILED` at 13:29:16Z with a no-produced-frame timeout, while current MainActivity/PID are not a live game. `sambas3-logs` requires evidence before retry; the distinct Luna collector owns W00-COLLECT-001 under a shared read-only lock. |
