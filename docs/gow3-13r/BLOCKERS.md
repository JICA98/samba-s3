# Current blockers and evidence gaps

| ID | Scope | Condition | Resolution owner and proof |
|---|---|---|---|
| B001 | Source-built release baseline | W00-BUILD-002 corrected guard and portable pinned core bundle passed independent Sol Max W00-REVIEW-003 for host-build input only. No core/APK has been compiled; missing nested fmtlib/dependency initialization could trigger submodule network work in the normal script. | Assign a separate source-build task with pinned inputs and deliberate dependency resolution; review actual core/APK artifacts before installation or performance claims. |
| B002 | Data-preserving device run | A selective 1,188-file external save/config/firmware backup is durable and hash-verified. App-private preferences and imported drivers remain inaccessible, so full rollback is unavailable. | User directed one observational test after risk disclosure; broader mutations still require a supported private-state export/rollback path. |
| B003 | Scored real-60 claim | Frame-level guest/render provenance and truthful evaluator are absent. | Luna build/metrics worker implements and tests W01; Sol reviewer validates before scoring. |
| B004 | Candidate attribution | The tester pulled the installed APK read-only; SHA-256 `c4bf9c1f4eb023fba8a78d22ee6b5ac00e8aa52a2e4e7ac3490cee7428f6ae46` differs from the preexisting host release `3bafe890…`. Loaded core/driver and effective settings remain unverified. | Tester records packaged core/signature and current runtime identities; build worker later supplies reviewed source/artifact manifest. Do not replace the installation merely to make hashes match. |
| B005 | Performance and save/load claim | The user reports GOW3 is playing, but the agents have not captured/attributed this live session; no UI state-restoration proof, raw power measurement or thermal soak exists. | Tester and collector produce immutable target evidence after coordinated phone access; reviewer evaluates exact data. |
| B006 | Thermal scoring readiness | The leased tester's raw battery value 361 corresponds to 36.1 °C and agrees with current ThermalHAL battery 36.1 °C; cached thermal-service battery 28.6 °C is stale. Thermal status was 0 at inventory. | Use fresh battery/HAL readings and the declared abort policy before and during any scored or sustained load; this single reading does not clear a later thermal gate. |
| B007 | Full private-state rollback | The installed release is non-debuggable; `run-as` and direct ADB reads of app-private `app_prefs` and imported `gpu_drivers` are denied. Historical logs show a driver label and user but the app was used afterward, so current values are unverified. Source shows MainActivity startup may reset an invalid selection and delete incomplete private driver entries. | The user directed one bounded observational boot after this risk was disclosed, but the full private-state gap remains. No install, driver/settings change, save/load stress or later comparative experiment until a supported export/rollback path is established. |
| B008 | Exclusive device control | During a fresh tester lease, a live `agy` session from another workspace sent Back input and captured screen/log evidence on the same USB phone outside the OS lock. The tester did not launch and released its lease. The bypassing child command ended, but the parent session remained live at the latest host check. | Do not cancel the other healthy agent. Obtain a coordinated exclusive window or verified terminal state for that actor, then reacquire a fresh lease and revalidate focus/identity/thermal state before passive capture of the user-reported running game. A quiet poll alone is not exclusivity proof. |

These conditions mark an incomplete W00 checkpoint. They are not evidence that the performance target is impossible.
