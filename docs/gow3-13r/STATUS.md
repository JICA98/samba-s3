# GOW3 / OnePlus 13R campaign status

Updated: 2026-09-23 13:17 UTC. State: `DISCOVERY`; readiness and benchmark acceptance remain incomplete. The independent Sol Max W00 review returned `NEEDS_EVIDENCE`. The user now reports GOW3 is playing, but no fresh agent capture verifies its runtime identity or performance; there is no accepted candidate, scored GOW3 run, device save/load test, or performance/energy result. The foreground `/root` session owns orchestration by explicit user direction; its verified runtime is GPT-6 Sol xhigh, a documented deviation from the planning pack's Sol Max request. The former Sol Max orchestration child has completed and no longer dispatches work.

The first guard/source bundle from W00-BUILD-001 was independently **rejected**: its release command could report success without a build, check-only could mutate persistent hunks, generated-only patches could pass, and the pinned core base object was not portable. The original submodule registration was restored without changing the original dirty source hashes. The same Luna High worker completed W00-BUILD-002 host corrections and an offline portable core bundle; the independent Sol Max W00-REVIEW-003 [approved](agents/reviews/W00-REVIEW-003.md) these exact inputs for a separate host source-build task only. No core or APK has been built from them.

The existing Luna High tester completed host-only backup preparation and a first read-only device inventory. USB identity matches model CPH2691 and SoC SM8650; the registered GOW3 entry is `direct_iso/BCUS98111`. A durable private backup of 1,188 selected PS3 save/config/firmware files (203,833,344 bytes) matched device-side SHA-256 and a second host copy; imported private driver/preferences files remain inaccessible. The installed APK differs from the preexisting host release. MainActivity startup may reset/delete invalid private driver state, and current selection/user cannot be proven from historical logs. After the user renewed the request to test with this risk disclosed, the tester reacquired a lock, but an active ADB client from another workspace sent input outside that lock. The tester did not launch, invalidated the run and released its lease. The user reports gameplay is now in progress, so the next device step is passive capture rather than relaunch, after a coordinated exclusive window. Raw serial and lock details remain private.

The verified planning pack is `SambaS3_GOW3_OnePlus13R_Worker_Agent_Pack` (`CHECKSUMS.sha256`: all entries pass). The acceptance contract is [acceptance.json](acceptance.json), registered before any scored experiment. The independent reviewer passed its fidelity for pre-registration at SHA-256 `7a58413ce32c82557d12272b7e4bd5d4df686667d1df08856042e0217e91c5b2` ([scoped verdict](agents/reviews/W00-REVIEW-002.md)); overall readiness remains `NEEDS_EVIDENCE`. The frozen JSON retains its creation-time pending-review field; this verdict records the later review without changing its hash. The gates cannot be weakened to promote a failed run.

## Verified W00 facts

| Item | Observation | Limit |
|---|---|---|
| Target | Tester archived fresh USB evidence for model CPH2691 (OnePlus 13R), SM8650 and Android SDK 36; raw serial remains private. | Loaded driver binary, full runtime settings and active PS3 user are still unverified. Device focus and thermals require revalidation before a new capture. |
| Game | Tester archived fresh registry evidence for `direct_iso/BCUS98111` with `DIRECT_ISO` installation type. User reports gameplay is in progress. | Exact update revision, executable/PPU hash, active content access and current frame source remain unverified. |
| App source | `master` at `8c95cb5b27a54d72328b993cd27fd8d5947c794d`; two modified tracked source files. Whole parent diff SHA-256 `0d92e24372b090fa9c0ace8b5b7811ff33f669969ee6424df8cbbfa25f97f14c`; source-only diff SHA-256 `e6bcb160ee0d02d2c9ebf33f38ab497a67d81559d63ea72dd401a341aa18476e`. | Preserve the dirty tree; the whole diff includes the submodule pointer display. |
| Core source | Gitlink and local core HEAD `657b26a0d197c29d42cdcf3b3f6e8ad5c6765bbc`; 40 modified tracked core files; core binary diff SHA-256 `ff3ecb0cc8d050a6935f6b499d72ff742d64b084affe5f4a677dce478f26ddda`. Offline base bundle and dirty-source reconstruction were independently approved for host-build input only. | No source-built core/APK yet; do not reset original edits or update to upstream HEAD. |
| Existing release artifact | Standard release APK: 92,401,079 bytes, SHA-256 `3bafe890309a92c023b588eaeba2f71817bda3c5d9b439eb37f6d867bc69b19f`; embedded ARM64 `librpcsx-android.so` SHA-256 `e6cfc98f35faad3172d46d5342cab80139666d2f4d71af38ccb2baa78b65f5eb`. | This preexisting artifact is not attributable to the dirty source. Installed APK and loaded core identities remain unknown. It is not a scored candidate. |
| Runtime routing | Trusted Codex state DB verifies the foreground coordinator as `openai/gpt-6-sol/xhigh`, independent reviewer as `openai/gpt-6-sol/max`, and existing build and tester workers as `openai/gpt-6-luna/high`; CLI 0.156.1. | Foreground xhigh differs from the planning pack's requested Max. Other defined roles remain unspawned. See [agent registry](agents/agent-registry.json). |
| Metric truth | Current bridge counts Vulkan/surface presentation callbacks without a verified unique guest/render frame identity; current capture/scorer cannot establish real GOW3 FPS. | No FPS or bottleneck claim may be made from the overlay, existing capture script, or demo summarizer. |
| Release build | W00-BUILD-002 corrected the generated-file patch guard and passed independent Sol Max review for host-build inputs only. | No source build has run; isolated offline nested dependencies and compiled artifact review remain. |

The tester verified USB identity and a selective durable backup, then released its lock after detecting an external ADB actor. The tester performed no APK installation, game launch, benchmark or scored save/load action in this W00 checkpoint. The user's new gameplay report belongs to a separate live session and still needs fresh evidence. The historical failed GOW3 session is archived separately and is not presented as this run.

## Candidate state

[candidate-baseline.json](candidate-baseline.json) distinguishes the unbuilt dirty source snapshot, the preexisting unattributed release APK, and the as-yet-unverified installed app. `current_best_accepted_candidate` is null. The old APK may be used only as a labeled untouched baseline after the tester verifies installation identity, protects user data and receives the device lease.

The frozen W00 source inputs are recorded in [source-input-manifest.json](source-input-manifest.json). The original dirty trees remain the authoritative source until a Luna worker creates and verifies an isolated reproducible bundle.

## Next bounded work

1. Coordinate an exclusive device window with the other live ADB actor; the user reports GOW3 is already playing, so preserve the session and use only scheduled passive evidence capture. No relaunch or gameplay input.
2. The reused Luna High build/metrics worker is running W01-BUILD-001: a strict isolated host evaluator rejecting demo, empty, repeated and presentation-only evidence. Independent method review follows; there is no scored benchmark yet.
3. Prepare a separate, reviewed host source-build task using the approved guard and offline bundle with deliberate nested-dependency handling. Do not install a different APK to force artifact identity.

Device work follows [W00 readiness and lease plan](W00_READINESS.md). All work and blockers stay visible in the [task index](agents/tasks.jsonl), [experiment ledger](EXPERIMENTS.csv), [decisions](DECISIONS.md) and [blockers](BLOCKERS.md).
