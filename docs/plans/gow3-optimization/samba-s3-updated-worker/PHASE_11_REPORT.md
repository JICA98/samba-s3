# Phase 11 — Turnip investigation (2026-09-26)

**Outcome: BLOCKED; no custom source build, driver import, or measured FPS improvement.** Phase 10 G2–G7 route and native-effective settings are still unverified (`PHASE10_11_REASSESSMENT.md:5-25`). A current-driver same-scene baseline cannot be certified, so a driver switch would not yield a valid A/B (`WORKER.md:605-646`; `PHASE_EVIDENCE_AUDIT.md:50-79`). No game was launched, no APK was built or installed, and no user data or drivers were changed.

## Read-only evidence

| Item | Observation |
|---|---|
| Devices | `adb devices -l`: OnePlus 13R `d30a1726` and Poco `Y5WWBMJVOZSK4HU8`, both `device`; `getprop ro.hardware`: `qcom` / `mt6897` respectively. Only OnePlus is an Adreno Turnip candidate. |
| OnePlus state | `dumpsys activity activities`: `MainActivity` top-resumed; app `versionCode=20260925`, `versionName=2026.09.25`; external `config/config.yml` has `Renderer: Vulkan`. Neither this nor a UI label establishes the library actually loaded during GOW3. Private `gpu_drivers/turnip-26.3/libvulkan_freedreno.so` cannot be read with shell (`Permission denied`). Current core mapping, runtime driver UUID, pipeline-cache UUID, patches, thermals, exact scene and frame-event window: unknown. |
| Existing importable archive | `app/src/main/assets/bundled_gpu_drivers/turnip-26.3-sambas3.zip`, SHA-256 `d0b0e1e923d42813751aa8ed96d4a6059137782f6a3901563efd9fdd847b6322`, 2,605,510 bytes. `unzip -l`: `meta.json`, `libvulkan_freedreno.so`, `SOURCE.txt`, `LICENSE-MESA`. ELF SHA-256 `fb0e7e155e003bb3edf8595386be2502a1b15ca632a48ae00a72222d930ed182`, 14,169,392 bytes, build ID `a3338b6245b1caf003d590a9512451b979f9fce6`. `readelf -d` requires Android `libhardware`, `liblog`, `libnativewindow`, `libsync`, `libm`, `libz`, `libdl`, `libc`. Archive is a historical packaged binary, **not a newly compiled Phase 11 candidate**. |
| Claimed source | Archive `SOURCE.txt` and `meta.json` plus `scripts/sync-bundled-turnip.sh:12-19` declare The412Banner/Banners-Turnip release `v26.3.0-20260826-r3`, upstream ZIP SHA-256 `94641a7e496f5d1f21d92d587d2f9336c0773582f38601e9d666b44240e3c8b8`, Mesa commit `bb63b1797b657b64dac605391d251a22e1c9cefc`, Vulkan `1.4.359`, KGSL A6xx/A7xx. The GitLab commit page responds HTTP 200. No upstream source-to-binary reproduction, exact downstream patch set, NDK/sysroot, Meson options or KGSL backend build configuration is included in this archive. The embedded commit string is a *claim*, not verified source-to-ELF lineage. |
| Loader/flavor | `docs/BUNDLED_TURNIP_DRIVERS.md:3-29`: app-private `gpu_drivers/<id>` and `adrenotools_open_libvulkan(CUSTOM, ...)`; `standard` allows external import; `playstore` disables it. The existing archive is copied under both `app/src/main/assets` and `app/src/playstore/assets`; it does not prove either installed APK's runtime driver. |

## Build and comparison gate

No patch/hotspot or matched GPU failure has been identified; no pinned source plus full dependencies and options was available to reproduce the existing release library. Inventing a new driver build from the stated commit would not satisfy the lineage or controlled-experiment requirements. `scripts/sync-bundled-turnip.sh` downloads/repackages the **prebuilt** binary and overwrites asset archives/catalogs; it was intentionally not run. `drivers/input/README.md` describes packaging inputs, not a Mesa source build. Existing control binary and game saves remain untouched.

Once Phase 10 yields a reproducible scene and frame-event boundary, record live core/loaded-driver identities (not labels), configuration, patch state, cache identity, thermals, scene images, and repeated control throughput. Obtain source and exact downstream patches/toolchain/KGSL build recipe for the intended candidate; build symbolized and release artifacts from the same pinned configuration, validate loader ABI and device compatibility, archive commands and SHA-256. Then import on the OnePlus through the standard-flavor path, verify actual library mapping/driver UUID, compare matching scene with reversible return to the control, and collect failure evidence once before any relaunch. Do not extrapolate OnePlus Turnip measurements to the Mali Poco.

**FPS result: unknown; no A/B, no improvement claim.**

## Follow-up source investigation (2026-09-26)

Public build recipe identified: [The412Banner/Banners-Turnip at `2d1ff8bb3dc1833b6d00075968e7fa5c7e28cf11`](https://github.com/The412Banner/Banners-Turnip/tree/2d1ff8bb3dc1833b6d00075968e7fa5c7e28cf11), specifically [`build_turnip_perf.sh`](https://github.com/The412Banner/Banners-Turnip/blob/2d1ff8bb3dc1833b6d00075968e7fa5c7e28cf11/build_turnip_perf.sh) and [the performance workflow](https://github.com/The412Banner/Banners-Turnip/blob/2d1ff8bb3dc1833b6d00075968e7fa5c7e28cf11/.github/workflows/turnip_build_perf.yml). Repository declares GPL-3.0; Mesa source has its own licensing. Recipe shallow-clones Mesa `main` (thus **not** a pinned Mesa revision), uses Android NDK download, `-O3 -flto=thin`, Meson `-Dplatforms=android -Dvulkan-drivers=freedreno -Dfreedreno-kmds=kgsl -Db_ndebug=true -Dstrip=true` and packages `libvulkan_freedreno.so` plus `meta.json`. Its workflow describes three inline NDK compatibility edits. Pin Mesa HEAD and record applied edits/NDK version before treating any build as reproducible; do not infer that the performance variant is faster from its flags. This candidate recipe differs from the historical `v26.3.0-20260826-r3` binary lineage.

Local NDKs 27.2, 29.0, 30.0; Meson and Ninja present. No pinned Mesa checkout, source build, newly importable ZIP, run IDs, loaded-driver proof, game-route completion or comparable FPS produced in this follow-up. Existing user changes in `GamepadMapper.kt`, `PadOverlay.kt`, Gaia status and Phase 10 reassessment left intact. The prior 12.728 FPS sample is historical, not a same-scene driver baseline. Driver iteration and >=30 FPS acceptance remain unverified.

## Build and bounded device continuation (2026-09-26)

**Build: succeeded. Performance: unverified.** Source: official `https://gitlab.freedesktop.org/mesa/mesa.git` commit `a5d39a4b743b719cfc2a0da410910ea3fd8ff770` (do not confuse with unrelated `github.com/mesa/mesa`, initially cloned and rejected). Build recipe pinned to The412Banner `2d1ff8bb3dc1833b6d00075968e7fa5c7e28cf11`, upstream script SHA-256 `4abb12cd2f12da7815cc29518757eff9c554e78684ba0fe390fa54aad866629b`. Local NDK `29.0.14206865`; three recipe NDK compatibility edits archived in `/tmp/sambas3-turnip-phase11/ndk-r29-compat.patch` (SHA-256 `14ae277b6f0824b9e750af2d0ae786cb224e14bbe747d827d0673a1a4ea62625`). Local recipe removes unavailable ccache, reuses installed NDK and pinned checkout, retains symbols (`-Dstrip=false`), uses `-O3 -flto=thin`, `-Dplatforms=android -Dvulkan-drivers=freedreno -Dfreedreno-kmds=kgsl -Dplatform-sdk-version=36`. Command: `SKIP_SOURCE_DOWNLOAD=1 BUILD_VERSION=phase11 bash /tmp/sambas3-turnip-phase11/build-local.sh > /tmp/sambas3-turnip-phase11/build.log 2>&1` (exit 0; `Build completed successfully!`). Exact local script and manifest: `/tmp/sambas3-turnip-phase11/{build-local.sh,manifest.json}`. License: recipe repository GPL-3.0; Mesa source has its own licenses.

Importable archive `/tmp/sambas3-turnip-phase11/sambas3-turnip-a7xx-a5d39a4.zip`, SHA-256 `c70d848f342ce2b4117cf516911e5ba6fac963d0565129de99cacc9125905fe2`, 4,095,764 bytes. AArch64 ELF SHA-256 `6d24bf67f5b9b11414ad33240cc641047adaaf4b392837abf6204da482a5067c`; GNU build ID `6b1ceef21e0b1840fa4531893b8ab2c46fc83a0c`; 19,915,936 bytes. `readelf` shows Android `libhardware`, `liblog`, `libnativewindow`, `libsync`, `libm`, `libz`, `libdl`, `libc`, matching control dependencies. Raw upstream archive and symbolized installed ELF retained separately. Driver/pipeline-cache UUIDs not retrieved.

OnePlus `d30a1726`, game `direct_iso/BCUS98111`: control boot accepted (`/tmp/sambas3-turnip-phase11/control-run-20260926`), Vulkan initialized on Turnip Adreno 750 driver `26.2.99`, thermal status 0. Control was stopped cleanly. Candidate ZIP staged in `/sdcard/Download`, imported through standard-app **IMPORT ADPKG** picker; driver card showed `SambaS3 Turnip A7xx a5d39a4-v1` selected, original Turnip 26.3 remained installed. First candidate boot reused PID 7914 and reported driver 26.2.99; **not** a valid new-driver trial (`candidate-run-20260926`). Stop broadcast timed out; captured packet `stop-timeout-20260926` before relaunch. MainActivity was focused; app process was then stopped, canonical warm-launch used again. Fresh PID 11327: `candidate-fresh-20260926/logcat-process.log` shows `RPCSX-UI: Loading custom driver /data/data/.../SambaS3 Turnip A7xx a5d39a4-v1` and `hook_impl: ... loading custom driver: .../libvulkan_freedreno.so`, then Vulkan initialization on Turnip Adreno 750. Reported version still `26.2.99` (insufficient to distinguish builds). `/proc/11327/maps` denied by Android shell; loader hook identifies selected path, **not ELF SHA/build ID**. Baseline and candidate `S3PERF export` events occur during boot (884 and 0 presented respectively), not matched gameplay intervals. No G2–G7 qualified scene, driver UUID, paired FPS, hotspot or demonstrated gain. No patch iteration justified yet. Original driver, installed app assets and game data preserved.

## Independent device continuation and audit (2026-09-26, ~11:00–11:08 device clock)

Fresh agent; previous work treated as evidence to verify, not a resumed test. Commands below executed from repository root. No source/build/APK changes; no release APK built or installed. Working tree already had edits in `GamepadMapper.kt`, `PadOverlay.kt`, `gaia-recovery-status.md` and untracked test/reassessment/report files; retained all. `adb devices -l` found OnePlus `d30a1726` and Poco `Y5WWBMJVOZSK4HU8` connected. OnePlus `games.json` independently confirmed `direct_iso/BCUS98111` as God of War III. Installed app reports `versionCode=20260925`, `versionName=2026.09.25`. Driver picker showed both packages installed; no driver deleted. No Poco test: Turnip targets OnePlus Adreno, not Poco Mali.

Artifact audit: `sha256sum /tmp/sambas3-turnip-phase11/sambas3-turnip-a7xx-a5d39a4.zip` reproduced `c70d848f342ce2b4117cf516911e5ba6fac963d0565129de99cacc9125905fe2` (4,095,764 bytes). Python `zipfile` enumerated `meta.json` (289 bytes) and `libvulkan_freedreno.so` (19,915,936 bytes); hashing decompressed ELF reproduced `6d24bf67f5b9b11414ad33240cc641047adaaf4b392837abf6204da482a5067c`. `git -C /tmp/sambas3-turnip-phase11/mesa-official rev-parse HEAD` reproduced `a5d39a4b743b719cfc2a0da410910ea3fd8ff770`; `build.log` ends `Build completed successfully!`. Additional artifact hashes: `build-local.sh` `b0b7e66fdf5b9a0d658acb07ee4d394cfc438eb7c96d13591f659758a18ef015`, `build.log` `51ad10bc77e5d84c320369675847f78a4eb38b1eeea1bb300c702855fcdf9028`, NDK patch `14ae277b6f0824b9e750af2d0ae786cb224e14bbe747d827d0673a1a4ea62625`, archived `control.so` `fb0e7e155e003bb3edf8595386be2502a1b15ca632a48ae00a72222d930ed182`. These verify *host artifacts*, not SHA of the sandboxed on-device installed library.

Chronology and evidence:

1. `./scripts/debug-launch-game.sh d30a1726 direct_iso/BCUS98111` refused: `RPCSXActivity is already focused`. Before any restart, `./scripts/get-samba-logs.sh d30a1726 /tmp/sambas3-turnip-phase11/independent-existing-20260926` captured that session. `agent-device --platform android --serial d30a1726 snapshot` showed an in-game PAUSED menu; `./scripts/debug-pad.sh d30a1726 CROSS` confirmed `DebugPad(11327): BUTTON CROSS consumed-by-frontend` plus release (`/tmp/samba-bridge-DoDzFG/logcat.txt`). Subsequent snapshot displayed live FPS, and `agent-device ... screenshot --out /tmp/sambas3-turnip-phase11/independent-candidate-milestone.png --no-stabilize` captured Kratos fighting a water creature in the Gaia route. Screenshot overlay showed **17.6 FPS, 55.8 ms, THERMAL SEVERE** at that instant; prior snapshot showed 22.8 FPS. `./scripts/get-samba-logs.sh ... /tmp/sambas3-turnip-phase11/independent-candidate-combat-20260926` collected current counters and thermal Status 3. Its `logcat-process.log` contains candidate PID 11327 `S3PERF` sampled `source=surface` counters (e.g. presented 2109, instantaneous json_fps 14.493 at 11:02:16; presented 2762, 15.3202 at 11:02:55). These are *uncontrolled instantaneous samples*, not qualified scene FPS. Earlier `candidate-fresh-20260926/logcat-process.log` at 10:56:29/31 explicitly names candidate selection and `hook_android_dlopen_ext` path then initializes Vulkan on Turnip Adreno 750. Same PID 11327 remained in the resumed session. Hook path proves a candidate path was requested/opened; Android shell denies `/proc/maps`, and the installed ELF hash/driver UUID remain unverified.
2. `./scripts/debug-stop-game.sh d30a1726` returned `stop completed ok=true` (`/tmp/samba-bridge-cnyVNM/logcat.txt`). In MainActivity Settings, agent-device picker snapshot showed candidate `SELECTED`; pressed original `Turnip 26.3 — Latest` card, then snapshot showed its `SELECTED` label. `./scripts/debug-launch-game.sh d30a1726 direct_iso/BCUS98111` accepted and focused (`/tmp/samba-bridge-9WFOvd/logcat.txt`); `./scripts/get-samba-logs.sh ... /tmp/sambas3-turnip-phase11/independent-control-warm-20260926` captured `hook_android_dlopen_ext: loading custom driver: /data/data/com.zenithblue.sambas3/files/gpu_drivers/turnip-26.3/libvulkan_freedreno.so` at 11:05:05, followed by `Vulkan: Renderer initialized on device 'Turnip Adreno (TM) 750'`. Driver path therefore switched on this boot despite PID 11327 reuse; same-PID assumption in prior paragraph is not itself proof of a driver remaining loaded. The control's *on-device ELF* hash is likewise unavailable.
3. `agent-device ... screenshot --out /tmp/sambas3-turnip-phase11/independent-control-milestone.png --no-stabilize` captured **Choose Difficulty** screen, not candidate's Gaia combat. Overlay showed 31.4 FPS/29.1 ms at that instant, THERMAL SEVERE; the counter is menu throughput, **not 30 FPS in Gaia**. `./scripts/get-samba-logs.sh ... /tmp/sambas3-turnip-phase11/independent-control-menu-20260926` captured thermal Status 3 and menu frame counter samples, not a paired gameplay scene. `./scripts/debug-stop-game.sh d30a1726` returned `stop completed ok=true` (`/tmp/samba-bridge-CHg8Cq/logcat.txt`). Original Turnip remains selected; game stopped cleanly. No game save/cache was cleared or overwritten deliberately.

Fault investigation: the collector's crash buffer includes an old `09:18:41` invalid-Surface PixelCopy exception and a **distinct 10:55:20 PID 7914 SIGTRAP** during the prior agent's timed-out stop (see `stop-timeout-20260926/logcat-crash.log`, `exit-info.txt`). Do not attribute either to this fresh candidate gameplay or call them Vulkan device loss. New evidence includes no timestamp-matched `VK_ERROR_DEVICE_LOST`; old rotated logs contain other unrelated events. First obstacle for A/B is *scene mismatch*: original boot landed at Choose Difficulty while candidate resumed existing Gaia combat; thermal Status 3 in both observations, no controlled thermal window, no exact effective config/cache/UUID confirmation. Selecting a difficulty would change gameplay progression; no blind navigation or second speculative launch. Do not infer 30 FPS or driver regression/improvement from 17.6 vs 31.4. No identified driver hotspot or matched driver failure warrants a Mesa patch. Next: establish repeatable G2–G7 route/checkpoint and scene boundaries on the same core/config/cache policy; verify actual installed binary/UUID if access is available, then alternate control/candidate at identical scene with thermal and full frame-event windows. Phase 11 remains **BUILD PASS / PERFORMANCE INCONCLUSIVE**.
