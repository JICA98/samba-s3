# Phase 10 / 11 reassessment — 2026-09-26

## Continued run — 2026-09-26

**Phase 10 incomplete; Phase 11 not authorized by its prerequisites.** Both devices were present at start. Existing focused session collected to `/tmp/gow3-phase10-20260926-existing`, then stopped cleanly using `./scripts/debug-stop-game.sh d30a1726` (`stop completed ok=true`, `/tmp/samba-bridge-FBhuUl/logcat.txt`). Bounded fresh `./scripts/debug-launch-game.sh d30a1726 direct_iso/BCUS98111` accepted and focused (`/tmp/samba-bridge-bK31HR/logcat.txt`). UI showed PPU compilation and a recovery overlay. Evidence collected before further input: `/tmp/gow3-phase10-20260926-first-fault`. `./scripts/debug-pad.sh d30a1726 CIRCLE` verified delivery (`/tmp/samba-bridge-h3vE7q/logcat.txt`). Further evidence: `/tmp/gow3-phase10-20260926-crash`. Its `logcat-crash.log:2-7` contains the concrete `09-26 09:18:41` main-thread `IllegalArgumentException: Surface isn't valid` from `PixelCopy.request`. Distinguish this timestamped crash from older rotated backend lines; the recovery overlay itself is not proof of a new native GPU failure.

`RPCSXActivity.kt:1830-1865` now catches an invalid-Surface `PixelCopy.request` race, recycles the bitmap and completes the frame callback with failure rather than crashing the UI. Ran `./gradlew :app:testStandardDebugUnitTest :app:testPlaystoreDebugUnitTest assembleStandardRelease --console=plain`: **BUILD SUCCESSFUL**, logs `/tmp/gow3-phase10-gradle.log`. Release APK SHA-256 `a362ea10d3c7906d3721103206d3c3fc575ca6855730d8acd6257e81823bb4e2`; packaged core provenance verified by `verifyStandardReleaseApk` (rpcsx `63e343bd8e4bf8b278fe1a32a5ebb2ca661b913c`, samba `e93784d8c4e4aac796b72a63a8f5a127affa29791b58b8a453aa74eae5c1460c`). `adb -s d30a1726 install -r ...apk` returned `Success`; game registry remained intact. Poco `Y5WWBMJVOZSK4HU8` disconnected during sequential install; its install command produced no success confirmation and subsequent `adb devices -l` showed only OnePlus. **Poco deployment remains unverified; reconnect and install the same release with `adb install -r`, no uninstall.**

Post-fix bounded OnePlus boot accepted and focused (`/tmp/samba-bridge-kpt2Qt/logcat.txt`); evidence `/tmp/gow3-phase10-20260926-postfix`. UI showed PPU/shader preparation and a `STOPPED UNEXPECTEDLY` recovery overlay. No verified G2–G7 route or exact native-effective settings; do not count a matrix cell. The post-fix crash buffer still contains the older `09:18:41` exception, not evidence of a post-fix recurrence. Native-effective settings and patch receipt remain as diagnosed below (`PatchFastMode.kt:519-525,559-590`; `WORKER.md:510-529`). No speculative effective-settings code was added: no identified native execution/readback hook yet; false attestation would invalidate the matrix. Phase 11 stays gated by `WORKER.md:621-647`; baseline, matched GPU fault, Turnip source lineage and driver binary identity are not established. No driver change or A/B.

Concurrent working-tree changes appeared during the build in `GamepadMapper.kt`, `PadOverlay.kt`, `RightStickAxisTest.kt`; they were not edited here. Preserve and review those before any further build. No saves/config were removed. References and original gates: `START_HERE.md`, `WORKER.md:306-355,510-529,601-647`, `PHASE_EVIDENCE_AUDIT.md`, `SOURCES.md`, `REVIEW_ADDENDUM.md`, `docs/benchmarks/2026-09-25-oneplus-13r-phase10-gow3.md`.

**Phase 10: INCOMPLETE. Phase 11: BLOCKED.** The initial no-device observation was stale. Recheck: `adb devices -l` listed `Y5WWBMJVOZSK4HU8` (Poco) and `d30a1726` (OnePlus), both `device`. No new matrix cell was executed. No APK was built or installed; user data was not changed.

## Phase 10

- The required Mega/Normal × optional-patches-on/off matrix, exact-settings verification and G0–G7 route gates remain outstanding (`WORKER.md:306-355`, `WORKER.md:601-620`). Do not treat the 2026-09-25 benchmark as a replacement: see `docs/benchmarks/2026-09-25-oneplus-13r-phase10-gow3.md` and `PHASE_EVIDENCE_AUDIT.md`.
- Device preflight: OnePlus `games.json` lists GOW3 as `direct_iso/BCUS98111`; `config/config.yml` reports `Renderer: Vulkan`. `./scripts/debug-launch-game.sh d30a1726 direct_iso/BCUS98111` refused with `RPCSXActivity is already focused. Collect evidence and exit the current game before a new boot.` Existing-session evidence: `/tmp/gow3-phase10-20260926-existing` (collector `scripts/get-samba-logs.sh`; `activity-state.txt` reports `topResumedActivity ... RPCSXActivity`). This is **not** a newly controlled G0–G7 run; existing session/settings cannot be assumed to match a matrix cell. No existing game was terminated or user configuration changed.
- The present receipt is **not native-effective evidence**: `PatchFastMode.kt:519-525` adds a name after `PatchRepository.setEnabled` succeeds; `:559-590` labels configured settings effective and counts those names as applied. The explicit correction requirement is `WORKER.md:510-529`; `REVIEW_ADDENDUM.md` disputes completeness of the prior latency distribution. No native patch execution or effective-settings readback was identified here. A cosmetic rename of the receipt would not establish runtime application.
- Next: finish the active session cleanly, establish a native readback of actual effective settings and patch application, cover disabled/mismatched/partial modes with meaningful tests, then run the exact controlled route/matrix and archive raw evidence. Do not delete game data merely to manufacture a cell.

## Phase 11

- No stable repeatable G2–G7 baseline, matched GPU fault or same-core/config/scene A/B was established. `WORKER.md:621-647` requires a diagnosed hotspot, source lineage, reproducible binary and controlled comparison before a Turnip specialization claim. `SOURCES.md:68` identifies a referenced core commit, **not** proof of the shipped GPU driver's source lineage. No speculative driver fork or binary replacement was made.
- Preparation: retain the existing benchmark as historical evidence, pin the actual installed driver binary/hash and source/KGSL requirements when the device and build inputs are accessible; only then design a diagnostic A/B if the route stabilizes or a matched GPU failure warrants it.

Commands observed: `rtk git status --short` (clean before documentation edits); `adb devices -l` (both connected on recheck); OnePlus `games.json` and `config.yml` read; bounded `debug-launch-game.sh` refused already-focused session; `get-samba-logs.sh` collected existing-session evidence. No test execution: no implementation changed. References: `START_HERE.md`, `WORKER.md:306-355,510-529,601-647`, `PHASE_EVIDENCE_AUDIT.md`, `SOURCES.md`, `REVIEW_ADDENDUM.md`, `docs/benchmarks/2026-09-25-oneplus-13r-phase10-gow3.md`.
