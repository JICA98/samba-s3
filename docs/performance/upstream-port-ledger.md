# SambaS3 Upstream Port Ledger (RPCS3 / RPCSX Synchronization)

This ledger tracks the evaluation, dependency mapping, and integration status of upstream RPCS3/RPCSX commits, PRs, and architectural enhancements into the SambaS3 core fork (`abhay-byte/samba-s3-core`, branch `samba-android`).

## Upstream Synchronization Table

| ID | Upstream Commit/PR | Subsystem | Present/Absent/Modified | Prerequisites | Conflicts | Android Impact | Result |
|---|---|---|---|---|---|---|---|
| **UP-01** | RPCS3 SPU AArch64 SIMD Lowering (PR #14205 / #15112) | SPU JIT (ARM64) | Present (Integrated) | Clang 18+, LLVM 20.1 | Android ABI calling conventions, veneer reachability | Direct `tbl2`/`tbl1` lowering for SHUFB; vector `cmtst` for FSM/FSMH/FSMB; immediate `zshuffle` for ROTQBYI/ROTQMBYI/SHLQBYI; passes `+neon` target attributes via `EngineBuilder.setMAttrs`. | ACCEPTED & INTEGRATED (Ticket J06 / E08) |
| **UP-02** | ARM SVE2 / FMS Vector Extensions | SPU JIT (ARM64) | Modified (Gated) | HWCAP2_SVE2 validation via `getauxval`, runtime CPU detection | Must not be enabled on CPUs without architectural SVE2 support (A520/A720/X4 compatibility check required) | Runtime-gated via `getauxval(AT_HWCAP)` / `getauxval(AT_HWCAP2)` to prevent illegal instruction crashes on Android kernels lacking SVE context switching; fallback to NEON. | ACCEPTED & GATED (Ticket J06 / E08) |
| **UP-03** | R15: Host busy-wait unsigned counter wrap & invariant TSC timer | Timing & Utilities (`rx/asm.hpp`, `rx/tsc.hpp`) | Present (Integrated) | ARM Generic Timer (19.2 MHz invariant TSC) | None | Fixes timer frequency miscalculation on ARM64; eliminates unsigned 32-bit counter wrap in host busy-wait loop. | ACCEPTED & VERIFIED (Commit `279dfafe` / `05ed8aed`) |
| **UP-04** | R09/R13: Ubertrampoline icache synchronization & single 32-bit atomic redirection | SPU JIT / Code Generation | Present (Integrated) | ARM cache maintenance instructions (`dsb ish`, `isb`) | +/- 128MB branch reachability on 64-bit address spaces | Emits clean d-cache and invalidate i-cache before CAS; uses 32-bit atomic store with nearby veneer for out-of-range targets. | ACCEPTED & VERIFIED (Commit `b1fa9a64`) |
| **UP-05** | R12: SPU LLVM compilation cache finalization barrier and W^X ordering | SPU JIT / Recompiler | Present (Integrated) | Thread-safe compilation queue | Potential reader race if compiled pointer stored before cache finalization | Enforces `dsb ish; isb` barrier and W^X memory protection prior to atomic store of compiled entry point. | ACCEPTED & VERIFIED (Commit `b1fa9a64`) |
| **UP-06** | R08: RSX TextureCache wait failure handling in `imp_flush()` | RSX / Vulkan | Present (Integrated) | Vulkan fence sync, DMA manager | Caller teardown and retry handling | Prevents memory corruption by retaining unsynchronized resources on wait failure instead of releasing them in dirty state. | ACCEPTED & VERIFIED (Commit `b1fa9a64`) |
| **UP-07** | R06/R07: PPU manifest integrity and MSELF sample digest verification | PPU / Storage | Present (Integrated) | SHA-256 digest helpers | Stale manifest re-use across app restarts | Treats empty or partial manifests as invalid, deleting them; verifies MSELF sample content digests to prevent stale compilation. | ACCEPTED & VERIFIED (Commit `70719886`) |
| **UP-08** | SPU Fast-Tier LLVM Recompiler Architecture | SPU Architecture (`SPUThread.cpp`) | Modified (Withdrawn on ARM64) | Fast recompiler pass pipeline for AArch64 | Upstream fast tier (`make_fast_llvm_recompiler`) is strictly x86_64 (`ARCH_X64`) | Clarified in `SPUThread.cpp`: ARM64 routes synchronously to `make_llvm_recompiler()`. Fast tier absent on ARM. | DOCUMENTED / RESEARCH (Target for J07 Stage C) |
| **UP-09** | CPU Topology Discovery and Heterogeneous Affinity Masking | Threading (`util/Thread.cpp`) | Present (Integrated) | Linux `/sys/devices/system/cpu` parsing | Hardware core cluster ordering | Supports OS mode, performance mask (`0xFC`), and custom masks intersected with allowed CPUs on Android. | ACCEPTED (Target for scheduling evaluation S05 / E03) |
| **UP-10** | Vulkan Readback and Staging Buffer Optimization | RSX / Vulkan | Absent (Upstream PR candidate) | Vulkan 1.3 sync2 | Android KGSL driver limitations | Minimizes staging copies and eliminates redundant CPU swizzle transformations during texture readbacks. | EVALUATING (Target for ticket V08 / E09) |
| **UP-11** | SPU LLVM runtime JIT single-flight compilation deduplication, barrier ordering, and cache key enhancements | SPU JIT / Recompiler (`SPURecompiler.h`, `SPULLVMRecompiler.cpp`, `SPUASMJITRecompiler.cpp`, `SPUThread.cpp`) | Present (Integrated) | Atomic compare-and-swap, futex wait/notify (`atomic_t`) | Multiple SPU threads entering uncompiled blocks concurrently causing CPU spikes / frame stalls | Implements atomic `spu_compile_state` per-block state machine so only 1 thread compiles duplicate block hashes while 5 concurrent workers wait; prompt LLVM IR/Module reclamation; cache key incorporates guest bytes SHA1, `s3cg1`, target CPU, and active SIMD features (`neon`/`sve2`); strict `dsb ish; isb` barrier prior to atomic publication. | ACCEPTED & INTEGRATED (Ticket J07 / E07) |

