# Plan Review: fgs-compile-notifications — PASS 1 ITER 1

## Verdict: REVISE
## Counts: CRITICAL 5 MAJOR 5 MINOR 2 SUGGESTIONS 2

### Findings
#### [CRITICAL] JNI JNIEnv thread-local misuse for native→Kotlin FGS callback
- Location: `docs/plans/fgs-compile-notifications-plan.md:96-97` (`rpcsx-android.cpp:1828 _rpcsx_setCompileProgressListener` + P3.4 `invokeAsync → g_mainThreadProcessor`)
- Problem: Plan caches/passes `JNIEnv*` from `g_mainThreadProcessor` thread (started at `MainActivity.kt:82-88`) and proposes invoking Kotlin callback via `CallVoidMethod`/`invokeAsync` from PPU (`progress_dialog_server`, `CompilationQueue::impl`) and RSX (`VKGSRender::check_cache_missed` at `VKGSRender.cpp:1987`, `GLGSRender.cpp:820`) threads without `JavaVM::AttachCurrentThread`. `JNIEnv*` is thread-local; reuse across threads is undefined.
- Evidence: `app/src/main/cpp/rpcsx/android/src/rpcsx-android.cpp:940-995` `class MainThreadProcessor` `process(JNIEnv* env)` stores env from initial `thread{ startMainThreadProcessor }` (`MainActivity.kt:82` / `RPCSXActivity.kt:67`). `app/src/main/cpp/rpcsx/rpcs3/Emu/system_progress.cpp:195-241` runs on `progress_dialog_server` thread; `app/src/main/cpp/rpcsx/rpcs3/Emu/RSX/VK/VKGSRender.cpp:1987-1996` runs on RSX thread. Plan P3.4 says `invokeAsync → g_mainThreadProcessor` or direct `CallVoidMethod on main looper`.
- Impact: `JNI DETECTED ERROR: use of invalid JNIEnv`, crash/ANR, lost FGS updates, `ForegroundServiceDidNotStartInTimeException` downstream. Will block M2/M3 manual tests.
- Required planner change: Specify `JavaVM* g_jvm` cached in `JNI_OnLoad`, `GlobalRef` for Kotlin `CompileProgressCallback` (`NewGlobalRef`), and per-callback `jvm->AttachCurrentThread(&env)` / `DetachCurrentThread` or `GetEnv` + `Handler(Looper.getMainLooper()).post{}`. Define native helper `void notifyCompileFg(int domain, long value, long max, std::string msg)` that does attach, `CallVoidMethod`, detach, never caches `JNIEnv`. Show exact header include and `ANativeWindow` thread analogy (`GraphicsFrame::getNativeWindow` wait loop).

#### [CRITICAL] Two notification-IDs with one FGS foreground ID demotes service prematurely
- Location: `docs/plans/fgs-compile-notifications-plan.md:73,107-110` `CompilationMonitorService NOTIF_ID_PPU=2001 NOTIF_ID_SHADER=2002` + `stopForeground`
- Problem: `ServiceCompat.startForeground(id, notification, type)` establishes *one* foreground notification. Second ID via `NotificationManagerCompat.notify(2002)` is not foreground. Calling `stopForeground(STOP_FOREGROUND_REMOVE)` when `ppu==max` while shader still active demotes service from foreground even though `2002` still shown, allowing system to kill background PPU->shader concurrent case (M4).
- Evidence: Plan P5: `For PPU: first event → createForeground(... NOTIF_ID_PPU) ... On isFinished → stopForeground ... Keep service alive while shader still active; stopSelf only when both done.` / `app/src/main/java/com/zenithblue/sambas3/ProgressRepository.kt:93-106` builds single builder+`notify(requestId.toInt())`. Android docs: only id passed to `startForeground` is foreground. `app/src/main/java/com/zenithblue/sambas3/PrecompilerService.kt:11` imports `ServiceCompat` but never uses it currently.
- Impact: M4 "Both concurrent" fails (`isForeground=false` while shader dot should remain), process killed when Home pressed during shader burst after PPU done, notification vanishes, `dumpsys activity services` shows wrong.
- Required planner change: Use single foreground ID (e.g., `FG_ID=2000` or reuse `2001` as canonical) and update same notification via `notify(FG_ID)` for both domains (merged `InboxStyle`/two lines `PPU 21/78 (10m)` + `Shaders compiling`). If two IDs required, define: only `2001` is foreground (started first), `2002` is regular `notify` while `isForeground` stays true via `2001`; `stopForeground` called *only* when both domains idle AND install flows idle. Document exact `ServiceCompat.startForeground`/`stopForeground` sequence and add assertion test `dumpsys notification | grep ongoing`.

#### [CRITICAL] Submodule boundary violation: editing upstream `rpcsx` files as app files
- Location: `docs/plans/fgs-compile-notifications-plan.md:67-69,131-134` File-Level Change Map `system_progress.cpp`, `overlay_compile_notification.cpp`, `VKGSRender.cpp`, `GLGSRender.cpp`
- Problem: Those files are under `app/src/main/cpp/rpcsx/` git submodule (`app/src/main/cpp/rpcsx/.git`, patch `patches/rpcsx-submodule-changes.patch` per `AGENTS.md`). Plan treats them as regular `app/src/main/cpp/rpcsx/android` modifications without patch maintenance, lacks include strategy (`g_compileFgCb` global defined in `rpcsx-android.cpp` not visible in `system_progress.cpp` without new header), violates layering.
- Evidence: `glob app/src/main/cpp/rpcsx/rpcs3/Emu/system_progress.cpp` exists inside submodule; `app/src/main/cpp/rpcsx/android/src/rpcsx-android.cpp:27` `#include "Emu/system_progress.hpp"` but no android header included in `system_progress.cpp:1-12`. `AGENTS.md` says `Submodule Patches | patches/rpcsx-submodule-changes.patch | Local changes to app/src/main/cpp/rpcsx`. No mention in plan except File-Level Change Map `MODIFY`.
- Impact: Worker edits will be clobbered on `git submodule update`, build breaks if patch not re-applied, upstream drift, CI doesn't apply changes. Also `handle_taskbar_progress` signature mismatch.
- Required planner change: Add **Submodule Patch Strategy** section: prefer callback via `Emu.GetCallbacks()` (`System.h:83 handle_taskbar_progress (type,value)` or new `handle_compile_progress(domain,value,max,string)`) wired in `rpcsx-android.cpp:1432-1451 setupCallbacks` instead of global `g_compileFgCb`. If `system_progress.cpp` must call FGS, expose via `Emu.GetCallbacks().handle_compile_progress` (extend `callbacks` struct) so no android header needed. Keep `overlay_compile_notification.cpp` hook via single chokepoint wrapper in android layer (don't edit VK/GL renderers; their `queue_message` already funnels to overlay). Document `git -C app/src/main/cpp/rpcsx apply $PWD/patches/...` step and update patch file path.

#### [CRITICAL] Cold-start service ownership gap (RPCSXActivity bypasses MainActivity)
- Location: `docs/plans/fgs-compile-notifications-plan.md:75,141` `MainActivity.kt ensureStarted` vs `RPCSXActivity.kt:59-75`
- Problem: Plan ensures `CompilationMonitorService` at `MainActivity` after `RPCSX.initialized`, but `RPCSXActivity` has cold-init fallback that bypasses MainActivity (force-stop → adb launch `sambas3.intent.action.Emulator`). Boot PPU compilation (progress_dialog_server) starts before service exists, so no FGS.
- Evidence: `app/src/main/java/com/zenithblue/sambas3/RPCSXActivity.kt:52-75` `// Cold-start library init fallback (skill sambas3-game-launch §2): direct RPCSXActivity after force-stop bypasses MainActivity.kt:28-86.` + `app/src/main/java/com/zenithblue/sambas3/MainActivity.kt:30-88` `RPCSX.initialized` threads. Plan `File-Level Change Map: MainActivity.kt ensureStarted` only.
- Impact: M2 "Boot PPU (cold boot game with no cache)" after force-stop shows RSX overlay only, shade entry missing → fails Acceptance Criteria `Progress: file X of Y (Xm remaining)` after Home.
- Required planner change: Add `CompilationMonitorService.ensureStarted()` in `RPCSXActivity.onCreate` immediately after `if (!RPCSX.initialized) { ... thread { startMainThreadProcessor } }` block (before `bootThread`), make `ensureStarted(ctx: Context)` idempotent via `applicationContext.startForegroundService` with `START_NOT_STICKY` fallback placeholder. Add lifecycle edge test: `adb shell am force-stop ... && adb shell am start -n .../.RPCSXActivity -e path ...` → dumpsys shows foreground.

#### [CRITICAL] FGS startForeground 10s timeout not met with event-driven deferral
- Location: `docs/plans/fgs-compile-notifications-plan.md:105-109,R1` `onCreate registers listener, first PPU event → createForeground`
- Problem: On Android 14+ (`targetSdk 35` `app/build.gradle.kts:19`) `startForeground` must be called within ~10s of `startForegroundService`/`startService`. Plan defers `createForeground`/`ServiceCompat.startForeground` until first native PPU/shader callback, which may arrive minutes later (user browses library, then launches game). Initial `startService` without `startForeground` crashes with `ForegroundServiceDidNotStartInTimeException`.
- Evidence: `app/build.gradle.kts:19` `targetSdk = 35`; `app/src/main/java/com/zenithblue/sambas3/PrecompilerService.kt:152` currently returns `START_STICKY` without FGS. Plan P5: `onCreate registers ... handle posts to Main handler ... For PPU first event → ProgressRepository.createForeground` and R1 mitigation says `call startForeground in onCreate with placeholder` but P5 step contradicts (first event, not onCreate). No placeholder notification defined.
- Impact: Crash in M2/M6 on Pixel/AOSP 14+, logcat `ForegroundServiceDidNotStartInTimeException`, blocks ship.
- Required planner change: Specify service immediately calls `startForeground(FG_ID, placeholderBuilder.build(), TYPE_DATA_SYNC)` in `onCreate` **and** `onStartCommand` (placeholder = indeterminate `Preparing compilation...` with `setProgress(0,0,true)` + `setOngoing(true)` + `setForegroundServiceBehavior(FOREGROUND_SERVICE_IMMEDIATE)` on API 34+). Subsequent native events update same FG_ID via `notify`. Keep timeout <5s, add `adb shell dumpsys activity services` check not crashing. Document `START_NOT_STICKY` vs `START_STICKY`.

#### [MAJOR] foregroundServiceType `dataSync` Play policy risk undefined
- Location: `docs/plans/fgs-compile-notifications-plan.md:64,82,180(a)` `foregroundServiceType="dataSync"` + `FOREGROUND_SERVICE_DATA_SYNC`
- Problem: `dataSync` is for user-initiated data transfer per Play policy. PPU/shader compilation is compute, not transfer; Play review may reject `dataSync` without justification. `specialUse` requires `<property android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE>` disclosure and Play console declaration. Plan asks reviewer to validate but leaves decision open, no per-flavor handling.
- Evidence: `app/src/main/AndroidManifest.xml:29-32` currently no `foregroundServiceType`. `app/build.gradle.kts:60-74` flavors `standard` vs `playstore` with different `ALLOW_EXTERNAL_GPU_DRIVERS`. Plan P1 says add `FOREGROUND_SERVICE_DATA_SYNC` or `specialUse if policy demands` without picking.
- Impact: `playstore` AAB rejection, rework after worker done.
- Required planner change: Pick `specialUse` for both flavors (or `dataSync` only if local policy proves exempt) and add manifest snippet: `<service android:foregroundServiceType="specialUse" ...><property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="ppuShaderCompilation"/></service>` + add `<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>`. Document Play console declaration and keep `standard` vs `playstore` parity. Cite Android 14 FGS types doc.

#### [MAJOR] Shader remaining estimation infeasible vs current indeterminate 5s hint
- Location: `docs/plans/fgs-compile-notifications-plan.md:95,172,180(c)` shader `g_shaderPending * ~120ms` / `InboxStyle` remaining
- Problem: No completion callback exists for shader pipeline compilation. `VKGSRender.cpp:1987-1997` / `GLGSRender.cpp:820-827` only gate `check_cache_missed()` → `show_shader_compile_notification()` (5s `queue_message` at `overlay_compile_notification.cpp:12`). No `m_prog_buffer->get_graphics_pipeline` completion timestamp or pending counter to decrement. `~120ms` estimate is guess without native hook.
- Evidence: `app/src/main/cpp/rpcsx/rpcs3/Emu/RSX/Overlays/overlay_compile_notification.cpp:12-27` defines `show_shader_compile_notification()` 5_000_000us. `app/src/main/cpp/rpcsx/rpcs3/Emu/RSX/VK/VKGSRender.cpp:1987-1997` shows single call with no decrement. No atomic `g_shaderPending` in repo (`grep g_shaderPending` = only plan).
- Impact: Worker will implement counter that never clears, notification stuck indeterminate forever, hide debounce 5s breaks M3, or remaining text flickers 0m.
- Required planner change: For v1 keep shaders strictly indeterminate: `setProgress(0,0,true)` + `setContentText("Compiling shaders")` + debounce show/hide 5s (no value/max). Remove `pending*120ms` formula or move to follow-up with explicit hook (e.g., wrap `VKGSRender::load_program` to increment/decrement via `Emu.GetCallbacks().handle_shader_compile` with proof via `git log --grep shader`). Update Acceptance Criteria to not require shader `value/max`.

#### [MAJOR] ProgressRepository.createForeground id mapping vs existing requestId generation
- Location: `docs/plans/fgs-compile-notifications-plan.md:86-89` P2 `createForeground(context: Service, title, notificationId)` + `store notificationId alongside ProgressWithHandler`
- Problem: Existing `ProgressRepository.kt:41-88` generates `requestId` via `nextRequestId++`, keys `ConcurrentHashMap<Long, ProgressWithHandler>`, `notify(requestId.toInt())`, `Handler` routes via `onProgressEvent(id,value,max)`. Plan mixes `notificationId` (Android notify int) with `requestId` (Long map key) without defining which is map key or how `onProgressEvent` updates FGS builder (reuses returned `requestId` vs fixed `NOTIF_ID_PPU`). Handler per-id `asyncHandler` at `ProgressRepository.kt:109` captures `builder` + `hasPermission` from creation closure—FGS path must not gate on `hasPermission`.
- Evidence: `app/src/main/java/com/zenithblue/sambas3/ProgressRepository.kt:76-157` `create` / `onProgressEvent:53` / `cancel:71`. No overload exists. `PrecompilerService.kt:112` `ProgressRepository.create(...) { entry.isFinished -> stopSelf }`.
- Impact: After first `onPpuProgress` `onProgressEvent(2001, ...)` returns `false` (no entry for 2001), FGS never updates, PPU progress stuck at 0, cancel never called. Or two maps diverge, leak `ProgressWithHandler`.
- Required planner change: Define exact API: `fun createForeground(service: Service, title: String, @Int notificationId: Int, initialMsg: String?): Long` inserts entry with **fixed key** = `notificationId.toLong()` (bypass `nextRequestId`) and stores `Service` + `ServiceInfo.FOREGROUND_SERVICE_TYPE_SPECIAL_USE` type; `onProgressEvent` already handles map lookup, but for FGS use `notify(notificationId)` not `requestId.toInt()`, and `asyncHandler` must capture `service` for `ServiceCompat.startForeground` immediate call and `stopForeground` on `isFinished`. Add diagram `notificationId (int) == requestId (Long)` for FGS, or separate `FgsHandle` class. Show pseudo-code and keep existing `create(Context)` unchanged.

#### [MAJOR] RPCSXActivity Compose mirror z-order/composition lifecycle underspecified
- Location: `docs/plans/fgs-compile-notifications-plan.md:115-118` P6 `RPCSXActivity` top chip observing `StateFlow`
- Problem: `RPCSXActivity.kt:83-97` already sets `binding.ingameOverlay` `ComposeView` `translationZ 64f` hosting `EmulationOverlayHost`. Plan adds "ComposeView top bar observing StateFlow" without ViewGroup slot, elevation vs `PadOverlay`, `WindowInsets` handling (`applyInsetsToPadOverlay` at `RPCSXActivity.kt:378-391`), or `collectAsState` lifecycle (`repeatOnLifecycle`). Risk chip behind `SurfaceView`/`PadOverlay` or leaks after `finish()`.
- Evidence: `app/src/main/java/com/zenithblue/sambas3/RPCSXActivity.kt:77` `setContentView(binding.root)` (`activity_rpcs3.xml` not read but hosts `padOverlay` + `ingameOverlay`). No chip exists. `RPCSXActivity.kt:378` `ViewCompat.setOnApplyWindowInsetsListener(binding.padOverlay)`.
- Impact: Chip invisible in M2/M3 screenshots, touches intercepted, rotation recreates without `configChanges` handled for chip, memory leak via `lifecycleScope` not cancelled.
- Required planner change: Specify insertion: add `<androidx.compose.ui.platform.ComposeView android:id="@+id/compileChip" android:elevation="65dp" android:layout_gravity="top|center"/>` to `activity_rpcs3.xml` `FrameLayout` above `padOverlay` sibling order, or overlay within `ingameOverlay` `Box` with `Modifier.zIndex(1f).statusBarsPadding()`. Collect via `lifecycle.repeatOnLifecycle(Lifecycle.State.STARTED) { viewModel.uiState.collect {}` } or `collectAsStateWithLifecycle`. Ensure `setViewCompositionStrategy(DisposeOnViewTreeLifecycleDestroyed)`. Add screenshot check in Testing Strategy.

#### [MAJOR] Submodule PPU progr forwarding via global vs handle_taskbar_progress mismatch
- Location: `docs/plans/fgs-compile-notifications-plan.md:94` `g_compileFgCb(pdone, ptotal, progr, PPU)` vs `handle_taskbar_progress [](auto...){}`
- Problem: `System.h:83` `handle_taskbar_progress = (type,value) type:0 reset 1 inc 2 set_limit 3 set_value` - not `(done,total,string)`. Plan suggests wiring `handle_taskbar_progress` to same FGS but `system_progress.cpp:193-387` computes `progr` string + `value = pdone*100/total` + `max=100` but current `handle_taskbar_progress` cannot carry string/time-remaining. Direct `g_compileFgCb` reachability from `system_progress.cpp` requires including android header.
- Evidence: `app/src/main/cpp/rpcsx/rpcs3/Emu/System.h:83` `std::function<void(s32,s32)> handle_taskbar_progress;` ; `app/src/main/cpp/rpcsx/rpcs3/Emu/system_progress.cpp:260-341` builds `progr` localized string; `app/src/main/cpp/rpcsx/android/src/rpcsx-android.cpp:1451` noop.
- Impact: Worker wired to `handle_taskbar_progress` will only forward `value` int, losing `(10m remaining)` text exactly required by Acceptance Criteria `Progress: file 21 of 78 ... (10m remaining)`. Direct global approach needs cross-TU linkage fix.
- Required planner change: Define new callback `std::function<void(u32 done,u32 total,std::string msg)> handle_compile_progress` in `Emu::callbacks` or extend existing to `(int type, s64 value, std::string msg)`. Wire in `setupCallbacks` at `rpcsx-android.cpp:1434` to `notifyCompileFg`. Have `system_progress.cpp:354-369` after `progr` final and `value` compute invoke `Emu.GetCallbacks().handle_compile_progress(pdone,ptotal,progr)` only when `show_overlay_message==false` and also when true (mirror). Keep overlay path unchanged. Show diff snippet.

#### [MAJOR] Permission POST_NOTIFICATIONS denied + channel importance handling contradictory
- Location: `docs/plans/fgs-compile-notifications-plan.md:83,112,R6` FGS bypass claim
- Problem: Plan says FGS notification still shows when POST_NOTIFICATIONS denied (system bypass on 14+), but also keeps `ProgressRepository.createForeground` silent/hasPermission gate. On API 33 POST_NOTIFICATIONS is required even for FGS? Behavior differs pre/post 34. `MainActivity.kt:34` channel `IMPORTANCE_DEFAULT` created once; FGS needs at least LOW but DEFAULT is ok—plan says "must be at least LOW" but doesn't enforce upgrade path if channel already exists with DEFAULT vs LOW.
- Evidence: `app/src/main/java/com/zenithblue/sambas3/MainActivity.kt:33-43` `NotificationChannel("rpcsx-progress", IMPORTANCE_DEFAULT)`; `app/src/main/java/com/zenithblue/sambas3/ProgressRepository.kt:91` `val hasPermission = !silent && checkPermission`. Plan R6: `FGS createForeground ignores gate; only setSilent respects it`.
- Impact: On API 33 with denied permission, `checkPermission` false → `hasPermission` false → `NotificationManagerCompat.notify` suppressed even though FGS should show, causing M5 failure. Channel recreation with different importance ignored after first create (system persists).
- Required planner change: Explicitly state `createForeground` never checks `Permission.PostNotifications` (remove `hasPermission` branch, always `ServiceCompat.startForeground` + `notify`), and add `if (Build.VERSION.SDK_INT < 34 && !hasPermission) requestPermission` fallback warning. For channel, check `if (existingChannel.importance == IMPORTANCE_NONE) recreate` or bump to `IMPORTANCE_LOW` with `createNotificationChannel` idempotent note and add `deleteNotificationChannel` migration if needed.

#### [MINOR] Citation line-number drift and symbol name typos
- Location: `docs/plans/fgs-compile-notifications-plan.md:4,10,25` `system_progress.cpp:195` `where of_1000, remaining` / `rpcsx-android.cpp:1264 CompilationQueue` / `1449 handle_taskbar_progress`
- Problem: `of_1000` at `system_progress.cpp:278` not 195 (195 is `while !stopping` loop). `handle_taskbar_progress` at `rpcsx-android.cpp:1451` not 1449. `VKGSRender.cpp:1990` off by 4 (actual 1987-1994). Causes navigation friction.
- Evidence: `app/src/main/cpp/rpcsx/rpcs3/Emu/system_progress.cpp:278` `const u32 of_1000 = ...` ; `app/src/main/cpp/rpcsx/android/src/rpcsx-android.cpp:1451` `.handle_taskbar_progress = [](auto...){}` ; `app/src/main/cpp/rpcsx/rpcs3/Emu/RSX/VK/VKGSRender.cpp:1987` `if (m_prog_buffer->check_cache_missed())`.
- Impact: Low - worker spends time searching wrong lines but still finds code.
- Required planner change: Correct all citations to verified lines: `system_progress.cpp:260-341 (of_1000:278, remaining:284, max_remaining:288, progr:269-341)`, `rpcsx-android.cpp:1264 CompilationQueue`, `:1451 handle_taskbar_progress`, `VKGSRender.cpp:1987-1996`, `GLGSRender.cpp:820-827`.

#### [MINOR] PrecompilerService START_STICKY vs FGS restart semantics
- Location: `docs/plans/fgs-compile-notifications-plan.md:31,113` `START_STICKY` + thumbnail
- Problem: Plan keeps `PrecompilerService` `START_STICKY` (currently `PrecompilerService.kt:151`) but FGS that finishes work should be `START_NOT_STICKY` to avoid system restarting with stale `intent Uri=null` after kill, which would hit `stopSelf` null-uri early-return path.
- Evidence: `app/src/main/java/com/zenithblue/sambas3/PrecompilerService.kt:100-151` `if (uri==null && batch==null) stopSelf; return START_STICKY`.
- Impact: Low - spurious restart attempts, extra empty notification.
- Required planner change: Specify `START_NOT_STICKY` for `CompilationMonitorService` and upgrade `PrecompilerService` to `START_NOT_STICKY` (or keep sticky but add null-intent guard test). Mention `onTaskRemoved`.

#### [SUGGESTION] Acceptance Criteria needs exact dumpsys grep for merged vs two-id
- Location: `docs/plans/fgs-compile-notifications-plan.md:160-163` `adb shell dumpsys notification | grep -A2 "Compiling PPU"`
- Problem: With single merged ID, grep must expect `InboxStyle` two lines; with two IDs need `grep -c "Compiling"` ==2. Plan lists both options but criteria says "two entries (or merged InboxStyle)" without single expected command.
- Evidence: `docs/plans/fgs-compile-notifications-plan.md:111` `Merge option: if UX prefers one notification...`
- Impact: Manual tester ambiguity.
- Required planner change: Make AC deterministic: `dumpsys notification | grep rpcsx-progress` must show `ongoing=true` and `grep "Compiling PPU"` contains remaining regex AND `grep "Compiling shaders"` exists when concurrent; if merged, verify `extras.textLines` length 2 via `dumpsys notification --noredact`.

#### [SUGGESTION] Missing lifecycle test for kill→re-boot rapid double startForeground
- Location: `docs/plans/fgs-compile-notifications-plan.md:180(e)` `MainActivity vs RPCSXActivity double startForeground`
- Problem: Plan flags but doesn't define expected behavior when user taps Back (kill → Stopped) then immediately re-launches game while service still foreground. Two rapid `ensureStarted` calls must be idempotent.
- Evidence: `app/src/main/java/com/zenithblue/sambas3/RPCSXActivity.kt:128-147` `bootThread` kill+busy-wait 300ms loop, `Emu.Kill()` async.
- Impact: `IllegalStateException: Service already in foreground` or duplicate placeholder.
- Required planner change: Add test M7: rapid kill→boot within 2s, expect no `ForegroundServiceDidNotStartInTimeException` and single ongoing notification, `adb logcat | grep -E "startForeground|STOP_FOREGROUND"`.

## Next Agent: Planner
## Next Action: Revise `docs/plans/fgs-compile-notifications-plan.md` to address all CRITICAL/MAJOR findings: fix JNI threading (JavaVM attach), single-FG-ID design, submodule patch strategy with callback, cold-start ownership in RPCSXActivity, immediate placeholder startForeground <5s, pick specialUse vs dataSync, make shaders indeterminate only, define createForeground id mapping, detail Compose chip slot/lifecycle, wire handle_compile_progress for progr string, and fix POST_NOTIFICATIONS bypass + channel importance. Re-run plan-reviewer.

